{
  "name": "demo-app",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Express demo app for CI/CD pipeline",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --check server.js && echo 'syntax check passed'"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
